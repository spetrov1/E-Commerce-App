// Stop MySQL80 service ... it is the default running local MySQL Server with the local databases

// MySQL init
// navigate to 'db' folder

docker build -t mysql-init-img .

docker run --name mysql-init --rm -d -p 3306:3306 mysql-init-img

// Spring Boot Backend Start
// Navigate to home directory of backend spring boot app

// Why not to execute this maven command within the dockerfile ????
./mvnw package && java -jar target/gs-spring-boot-docker-0.1.0.jar

docker build -t e-app-img .

docker run --name e-app -p 8080:8080 .


// TODO Angular Start